<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Password Updated</title>
    <style>
        :root { color-scheme: dark; font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; --bg:#121212; --panel:#212121; --muted:#b3b3b3; --text:#f5f5f5; --accent:#1db954; --border:rgba(179,179,179,.18); }
        * { box-sizing: border-box; }
        body { margin:0; min-height:100vh; background:var(--bg); color:var(--text); display:grid; place-items:center; padding:24px; }
        main { width:min(100%,460px); background:var(--panel); border:1px solid var(--border); border-radius:20px; padding:24px; text-align:center; }
        .icon { width:56px; height:56px; border-radius:28px; background:rgba(29,185,84,.16); color:var(--accent); display:grid; place-items:center; margin:0 auto 16px; font-size:30px; font-weight:900; }
        h1 { margin:0 0 8px; font-size:30px; }
        p { margin:0; color:var(--muted); line-height:1.5; }
    </style>
</head>
<body>
    <main>
        <div class="icon">✓</div>
        <h1>Password updated</h1>
        <p>Your password was changed successfully. You can return to the app and log in.</p>
    </main>
</body>
</html>
